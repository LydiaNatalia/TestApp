// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.rpll.app.model;

import com.rpll.app.model.PegawaiDataOnDemand;
import com.rpll.app.model.Staff;
import com.rpll.app.model.StaffDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect StaffDataOnDemand_Roo_DataOnDemand {
    
    declare @type: StaffDataOnDemand: @Component;
    
    private Random StaffDataOnDemand.rnd = new SecureRandom();
    
    private List<Staff> StaffDataOnDemand.data;
    
    @Autowired
    private PegawaiDataOnDemand StaffDataOnDemand.pegawaiDataOnDemand;
    
    public Staff StaffDataOnDemand.getNewTransientStaff(int index) {
        Staff obj = new Staff();
        setJabatan(obj, index);
        setPassStaff(obj, index);
        return obj;
    }
    
    public void StaffDataOnDemand.setJabatan(Staff obj, int index) {
        String jabatan = "jabatan_" + index;
        if (jabatan.length() > 40) {
            jabatan = jabatan.substring(0, 40);
        }
        obj.setJabatan(jabatan);
    }
    
    public void StaffDataOnDemand.setPassStaff(Staff obj, int index) {
        String passStaff = "passStaff_" + index;
        if (passStaff.length() > 20) {
            passStaff = passStaff.substring(0, 20);
        }
        obj.setPassStaff(passStaff);
    }
    
    public Staff StaffDataOnDemand.getSpecificStaff(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Staff obj = data.get(index);
        String id = obj.getNip();
        return Staff.findStaff(id);
    }
    
    public Staff StaffDataOnDemand.getRandomStaff() {
        init();
        Staff obj = data.get(rnd.nextInt(data.size()));
        String id = obj.getNip();
        return Staff.findStaff(id);
    }
    
    public boolean StaffDataOnDemand.modifyStaff(Staff obj) {
        return false;
    }
    
    public void StaffDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Staff.findStaffEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Staff' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Staff>();
        for (int i = 0; i < 10; i++) {
            Staff obj = getNewTransientStaff(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
